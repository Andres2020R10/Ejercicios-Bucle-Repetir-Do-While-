Proceso PromedioExamenes
    Definir nota, suma, promedio Como Real
    Definir contador, continuar Como Entero
    suma <- 0
    contador <- 0
    
    Repetir
        Escribir "Ingrese la nota del examen:"
        Leer nota
        suma <- suma + nota
        contador <- contador + 1
        
        Escribir "¿Desea ingresar otra nota? (1=Sí / 0=No):"
        Leer continuar
    Hasta Que continuar = 0
    
    promedio <- suma / contador
    
    Escribir "El promedio de notas es: ", promedio
    Si promedio >= 3.0 Entonces
        Escribir "El estudiante ha aprobado."
    Sino
        Escribir "El estudiante ha reprobado."
    FinSi
FinProceso
